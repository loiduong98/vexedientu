const stateUser = {
    credentials:null
}

const userReducer = (state = stateUser, action) => {
    switch (action.type) {
        default:
            return state;
    }
}
export default userReducer;
