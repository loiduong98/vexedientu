export default class Bus {
  constructor(id, name, owner, amount, time, source, destination) {
    this.id = id;
    this.name = name;
    this.owner = owner;
    this.amount = amount;
    this.time = time;
    this.source = source;
    this.destination = destination;
  }
}
